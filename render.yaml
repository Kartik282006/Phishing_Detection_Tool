databases:
  - name: phish-carnage-db
    plan: free
    databaseName: phish_carnage
    user: phish_carnage_user

services:
  - type: web
    name: phish-carnage
    env: python
    rootDir: phishing
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: phish-carnage-db
          property: connectionString
